import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

@SuppressWarnings("serial")
public class LoginMenu extends JPanel implements ActionListener
{
	private JLabel welcome = new JLabel("Welcome to eMenu");
	private JButton customer = new JButton("Browse/Order Now");
	private JButton login = new JButton("Staff Login");
	
	private JFrame f;
	
	public LoginMenu()
	{
		f = new JFrame("Login");
		f.setVisible(true);
		f.setSize(1000, 800);
		f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		JPanel p = new JPanel();
		p.setLayout(new FlowLayout());
		p.setBackground(Main.bGroundColor);
		
		p.add(welcome);
		p.add(customer);
		p.add(login);
		login.setPreferredSize(new Dimension(1000, 300));
		welcome.setPreferredSize(new Dimension(1000, 200));
		login.addActionListener(this);
		customer.addActionListener(this);
	
		f.add(p);
	}
	
	
	public void actionPerformed(ActionEvent a)
	{
		if(a.getSource() == customer)
		{
			new MainMenu();
			f.dispose();
		}
		if(a.getSource() == login)
		{
			new StaffLogin();
			f.dispose();
		}
	}
}
